import 'package:flutter/material.dart';

class ViewPatientRecord extends StatelessWidget {
  const ViewPatientRecord({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
